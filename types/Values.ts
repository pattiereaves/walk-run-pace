export type Values = {
  event: {
    /** Distance in miles. */
    distance: number;
    /** Duration in seconds. */
    duration: number;
  },
  intervalDuration: {
    /** Duration in seconds. */
    run: number;
    /** Duration in seconds. */
    walk: number;
  };
};
